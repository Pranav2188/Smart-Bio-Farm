# Production Environment Configuration
# Copy this file to backend/.env when deploying to production

# Server Configuration
PORT=10000
NODE_ENV=production

# Admin Setup Code for Government Signup
# IMPORTANT: Change this to a secure code before deploying!
ADMIN_SETUP_CODE=PROD_CODE_2025_CHANGE_THIS

# Firebase Configuration
# For Render deployment, use the single-line JSON format from firebase-credentials-for-render.txt
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Deployment Configuration
RENDER_SERVICE_NAME=trip-defender-backend
RENDER_REGION=oregon
RENDER_PLAN=free

# Health Check Configuration
HEALTH_CHECK_PATH=/
HEALTH_CHECK_TIMEOUT=30000

# Auto Deploy Configuration
AUTO_DEPLOY_BRANCH=main

# JWT Secret (for future implementation)
# IMPORTANT: Use a strong, randomly generated secret in production!
# JWT_SECRET=production_jwt_secret_CHANGE_THIS_TO_RANDOM_STRING
